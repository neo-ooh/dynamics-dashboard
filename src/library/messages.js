import app from './messages/app'
import params from './messages/params'
import weather from './messages/weather'

const messages = {
  app: app,
  params: params,
  weather: weather,
}

export default messages
